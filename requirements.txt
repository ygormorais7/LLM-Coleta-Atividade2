# --- núcleo ---------------------------------------------------------------
requests>=2.32
beautifulsoup4>=4.12
lxml>=5.2
PyYAML>=6.0
pandas>=2.2
pyarrow>=16.0

# --- extração de texto ----------------------------------------------------
PyMuPDF>=1.24          # extração rápida + rasterização para OCR
pypdf>=4.2             # fallback para PDFs que o PyMuPDF recusa

# --- normalização ---------------------------------------------------------
ftfy>=6.2              # correção de mojibake
langdetect>=1.0.9      # detecção de idioma com probabilidade

# --- deduplicação ---------------------------------------------------------
datasketch>=1.6        # MinHash + LSH

# --- testes e documentação -----------------------------------------------
pytest>=8.0            # python -m pytest
markdown>=3.5          # docs/gerar_pdf_relatorio.py (usa também o LibreOffice)

# --- opcionais (descomente conforme o config) -----------------------------
# pytesseract>=0.3.10  # OCR   -> exige o binário: apt install tesseract-ocr tesseract-ocr-por
# Pillow>=10.3         # necessário junto com pytesseract
# docling>=2.0         # extrator alternativo, mais preciso e mais lento
# selenium>=4.20       # fallback para repositórios que montam links via JS
# spacy>=3.7           # NER de nomes; depois: python -m spacy download pt_core_news_lg
# duckdb>=1.0          # exploração SQL direta nos Parquet
# zstandard>=0.22      # compressão dos shards de pré-treino
